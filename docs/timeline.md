a place for me to note down the things that I have to do:
- need to integrate the llm's and all that and test if it's working (loading into VRAM and all that)
- check the same with the embedding model
- need to set a place for the vdb to go (both chroma and faiss) (given the option, need to make a simple thing that switches between them, the implementation shouldnt be that diff i hope)
- need to hardcode a local location of the database to go into
- need to find a good / decent data set that i can use and also use it to ingest new documents 
- need to find out what the best data analysis methods are - I need to do 5, and save their graph outputs to a folder and link to it in the documentation
- need to do data cleaning and note what things I did, preferably use a data cleaning pipeline / a .ipynb
- make sure the incremental loading pipeline works
- work in parquet instead of CSV for loading the files
- find the best chunking strats
- set up chroma DB
- find what are the good ways to do (unit) testing and start setting that up
- build the query re-writer
- need to build a pipeline for the AI, have it share routes with the RAG as well (ollama should handle that)
- try to see if I can inject AI Agents into this (and if I do, how do i do that)
- set up the retreiver, try to see if I can have other things as well
- have this ensemble retreiver as well with the normal "cosine" similarity search
- find out good ways to benchmark / eval the RAG as well as the retreival / chunking techniques that I will be using